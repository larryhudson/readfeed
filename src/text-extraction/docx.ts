export async function extractChaptersFromDocx(docxFilePath) {
  console.log("Extracting chapters from DOCX");
  // if the document has heading levels, use those to split up the document
  // use the same logic as webpages - look for the highest heading level with multiple entries
  // use mammoth to convert the DOCX into HTML
  // do some processing with cheerio
  // convert to text with html-to-text
  // return the chapters
}
